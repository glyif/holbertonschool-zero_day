"How to use VMs with Vagrant"

When propogating a vagrant box, vagrant actually creates a vm through VirtualBox. If you run the commands to create a vagrant instance you can then go on VirtuaBox's GUI and see the intance you created.
